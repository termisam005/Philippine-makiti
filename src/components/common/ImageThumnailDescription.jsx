import React from 'react';

const ImageThumnailDescription = ({ children }) => {
  return (
    <div className={'thumbnail_description m-t-16'}>
        {children}
    </div>
  );
};

export default ImageThumnailDescription;